export interface AdminAccount {
  id: string;
  fullName: string;
  email: string;
  username: string;
  password: string;
  createdAt: string;
}

export interface HotelSettings {
  name: string;
  address: string;
}
